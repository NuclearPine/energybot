Telegram bot that automatically publishes a table of weekly petroleum market information from the US EIA. Utlizes AWS Lambda and DynamoDB

This was primarily a learning project and is no longer active on Telegram, to avoid consuming AWS resources.
